<?php
// Опишите как бы вы реализовали взаимодействие системы Библиотека с системой Учета, в которой есть операции Купить книгу,
// Дать книгу в пользование, Просмотреть каталог книг, но при этом информация о книгах хранится в системе Библиотека


namespace library;

// Класс, представляющий систему Библиотека
class LibrarySystem {
    // Метод для получения информации о книге по идентификатору
    public function getBookInfo($bookId) {
        // Логика для вызова API или сервиса Библиотеки для получения информации о книге
        // Возвращается информация о книге в виде объекта или массива данных
    }

    // Метод для регистрации операции покупки книги
    public function purchaseBook($bookId) {
        // Логика для вызова API или сервиса Библиотеки для регистрации операции покупки книги
        // Возвращается результат операции (успех или ошибка)
    }

    // Метод для регистрации операции выдачи книги в пользование
    public function lendBook($bookId) {
        // Логика для вызова API или сервиса Библиотеки для регистрации операции выдачи книги в пользование
        // Возвращается результат операции (успех или ошибка)
    }

    // Метод для получения списка всех книг в каталоге библиотеки
    public function getAllBooks() {
        // Логика для вызова API или сервиса Библиотеки для получения списка всех книг в каталоге
        // Возвращается список книг в виде объекта или массива данных
    }
}

